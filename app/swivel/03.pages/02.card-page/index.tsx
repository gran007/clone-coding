'use client';

import style from './style.module.css';

const CardPage = ({ show }: { show: boolean }) => {
    return (
        <section className={style.cardSection}>
            
        </section>
    )
}

export default CardPage;